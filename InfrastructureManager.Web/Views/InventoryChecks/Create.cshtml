@model InfrastructureManager.Web.ViewModels.InventoryChecks.CreateInventoryCheckViewModel

@{
    ViewData["Title"] = "New Check";
}

<div class="page-header">
    <div>
        <div class="breadcrumb-nav">
            <a asp-action="Index" asp-route-departmentId="@Model.DepartmentId">Checks</a>
            <span>/</span><span>New</span>
        </div>
        <h1 class="page-heading">Check — @Model.DepartmentName</h1>
        <div class="text-secondary small">@Model.LocationName</div>
    </div>
</div>

@if (!Model.Items.Any())
{
    <div class="content-card">
        <div class="empty-state py-4">
            <div class="empty-icon"><i class="bi bi-inboxes"></i></div>
            <div class="empty-title">No devices</div>
            <div class="empty-description">This department doesn't have any devices to check yet.</div>
        </div>
    </div>
}
else
{
    <form asp-action="Create" method="post" enctype="multipart/form-data">
        @Html.AntiForgeryToken()
        <input type="hidden" asp-for="DepartmentId" />
        <input type="hidden" asp-for="DepartmentName" />
        <input type="hidden" asp-for="LocationName" />

        <div class="content-card mb-4">
            <div class="details-section-title">General notes</div>
            <textarea asp-for="Notes" rows="2" class="form-control custom-input"
                      placeholder="Context or particulars for this check..."></textarea>
        </div>

        <div class="content-card mb-4">
            <div class="details-section-title">Devices (@Model.Items.Count)</div>
            <div class="d-flex flex-column gap-3">
                @for (int i = 0; i < Model.Items.Count; i++)
                {
                    var item = Model.Items[i];
                    <div class="content-card" style="background:var(--color-surface-subtle)">
                        <input type="hidden" asp-for="Items[i].DeviceId" />
                        <input type="hidden" asp-for="Items[i].DeviceName" />
                        <input type="hidden" asp-for="Items[i].DeviceType" />

                        <div class="row g-3 align-items-center">
                            <div class="col-md-3">
                                <div class="fw-semibold">@item.DeviceName</div>
                                <div class="text-secondary small">@item.DeviceType</div>
                            </div>
                            <div class="col-md-2">
                                <div class="form-check form-switch">
                                    <input asp-for="Items[i].IsPresent" class="form-check-input" id="present_@i" />
                                    <label class="form-check-label" for="present_@i">Present</label>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <textarea asp-for="Items[i].Remark" rows="1" class="form-control custom-input"
                                          placeholder="Remark (optional)"></textarea>
                            </div>
                            <div class="col-md-3">
                                <input type="file" asp-for="Items[i].Photo" class="form-control"
                                       accept="image/jpeg,image/png,image/gif,image/webp" />
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

        <div class="form-actions">
            <a asp-action="Index" asp-route-departmentId="@Model.DepartmentId" class="btn btn-light custom-btn">Cancel</a>
            <button type="submit" class="btn btn-primary custom-btn">
                <i class="bi bi-check-lg"></i> Save Check
            </button>
        </div>
    </form>
}