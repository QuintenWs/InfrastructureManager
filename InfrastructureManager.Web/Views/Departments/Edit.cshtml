@model InfrastructureManager.Web.ViewModels.Departments.UpdateDepartmentViewModel

@{
    ViewData["Title"] = "Edit Department";
}

<div class="page-header">
    <div>
        <div class="breadcrumb-nav">
            <a asp-action="Index">Departments</a><span>/</span><span>Edit</span>
        </div>
        <h1 class="page-heading">Edit Department</h1>
    </div>
</div>

<div class="form-page">
    <div class="form-card">
        <form asp-action="Edit" method="post">

            <input asp-for="Id" type="hidden" />
            <partial name="_ValidationSummary" />

            <div class="form-grid">

                <div class="form-group">
                    <label asp-for="LocationId"></label>
                    <select asp-for="LocationId" asp-items="Model.Locations" class="form-select custom-input">
                        <option value="">Select Location</option>
                    </select>
                    <span asp-validation-for="LocationId" class="text-danger small"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Name"></label>
                    <input asp-for="Name" class="form-control custom-input" />
                    <span asp-validation-for="Name" class="text-danger small"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Address"></label>
                    <input asp-for="Address" class="form-control custom-input" />
                    <span asp-validation-for="Address" class="text-danger small"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Description"></label>
                    <input asp-for="Description" class="form-control custom-input" />
                </div>

            </div>

            <div class="form-group mt-3">
                <label asp-for="Notes"></label>
                <textarea asp-for="Notes" rows="3" class="form-control custom-input"></textarea>
            </div>

            <div class="form-actions mt-4">
                <a asp-action="Index" class="btn btn-light custom-btn">Cancel</a>
                <button type="submit" class="btn btn-primary custom-btn">
                    <i class="bi bi-check-lg"></i> Save Changes
                </button>
            </div>

        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
